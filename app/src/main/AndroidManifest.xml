<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_conectando_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_conectando_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>

        <!-- ESTUDIANTE -->

        <activity android:name=".StudentActivity"/>
        <activity android:name=".HistoryActivity"/>
        <activity android:name=".PaymentActivity"/>
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".JobsActivity"/>
        <activity android:name=".PostulateStudentActivity"/>



        <!-- EMPRESA -->
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".EmpresaActivity"/>
        <activity android:name=".CrearOfertaActivity" />
        <activity android:name=".VerPostulacionesActivity" />
        <activity android:name=".EditarPostulacionActivity" />
        <activity android:name=".PagoPostulacionActivity" />
        <activity android:name=".ListarPostulacionEdit" />

        <!-- MENTORIAS -->
        <activity android:name=".CrearMentoriaActivity"/>
        <activity android:name=".MentorActivity" />
        <activity android:name=".MentorProfileActivity" />
        <activity android:name=".EliminarPostulacionActivity" />
        <activity android:name=".SupportActivity"/>
        <activity android:name=".VisualMentoriaActivity"/>
        <activity android:name=".MentorsActivity" />
        <activity android:name=".AgregarContenidoMentoriaActivity" />
        <activity android:name=".VerContenidosMentoriaActivity" />


        <!-- ADMIN -->
        <activity android:name=".AdminActivity"/>
        <activity android:name=".JobOffersActivity"/>
        <activity android:name=".UserManagementActivity"/>
        <activity android:name=".AdminPostulacionesActivity"/>








    </application>

</manifest>